# Material examples (shaders only)

`.tres` files here reference only addon shaders. Assign your own albedo, normal, ORM, and displacement textures in the Inspector after creating a `ShaderMaterial` from the matching `.gdshader` in `../shaders/`.

The addon does not ship demo texture assets.
